using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySpawnSystem : MonoBehaviour {

	public EnemyAttributes enemieSpawnables;

	private List<Light> myLights;

	//just spawning lights to test this whole scriptable objects thing.
	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		//logic for what they do, how where they run to.
	}
}
